var typed = new Typed(".text", {
  strings: ["Fullstack Developer", "PHP Developer"],
  typeSpeed: 70, // Slower typing speed
  backSpeed: 50, // Slower backspacing speed
  backDelay: 1500, // Longer delay before backspacing
  loop: true,
});
